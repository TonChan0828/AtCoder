#include <bits/stdc++.h>
using namespace std;

#define rep(i, n) for (int i = 0; i < (int)(n); i++)

int main()
{
  int N, M, K;
  cin >> N >> M >> K;

  long long count = 0;
  rep(i, )
          cout
      << count << endl;
  cout << (count % 998244353) << endl;

  return 0;
}
